# defaults.yaml
from: markdown
to: pdf
# defaults.yaml (Settings පමණි)
output-file: emotional_intelligence_sl.pdf
pdf-engine: xelatex
variables:
  mainfont: "LKLUG"
  fontsize: 12pt
  geometry:
    - paperwidth=6in
    - paperheight=9in
    - margin=0.75in
  lang: si-LK

output-file: emotional_intelligence_sl.pdf
pdf-engine: xelatex

variables:
  mainfont: "LKLUG"
  fontsize: 12pt
  lang: si
  polyglossia-lang:
    name: sinhala
  geometry:
    - paperwidth=6in
    - paperheight=9in
    - margin=0.75in

metadata:
  # ලේඛනයේ ඇති ඕනෑම backslash ගැටලුවක් මඟහරවා ගැනීමට
  header-includes: |
    \usepackage{fontspec}
    \defaultfontfeatures{Mapping=tex-text}
    
