# defaults.yaml
from: markdown
to: pdf
# defaults.yaml (Settings පමණි)
output-file: emotional_intelligence_sl.pdf
pdf-engine: xelatex
variables:
  mainfont: "LKLUG"
  fontsize: 12pt
  geometry:
    - paperwidth=6in
    - paperheight=9in
    - margin=0.75in
  lang: si-LK

output-file: emotional_intelligence_sl.pdf
pdf-engine: xelatex

# variables සහ metadata එකිනෙකට වෙනස් වේ
variables:
  fontsize: 12pt
  geometry:
    - paperwidth=6in
    - paperheight=9in
    - margin=0.75in

# header-includes කොටස metadata යටතට ඇතුළත් කරන්න
metadata:
  header-includes: |
    \usepackage{fontspec}
    \usepackage{polyglossia}
    \setmainlanguage{sinhala}
    \setmainfont{LKLUG}
    
  
